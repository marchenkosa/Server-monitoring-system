server:
  url: "http://10.10.10.222:5000"
  endpoint: "/api/metrics"
  timeout: 10

agent:
  name: "Windows-Server-01"
  interval: 60  # seconds
  retry_attempts: 3

monitoring:
  services:
    - "nginx"
    - "mysql"
    - "apache"
    - "W3SVC"
    - "MSSQLSERVER"
  disks:
    - "C:"
    - "D:"